# Excepciones al estado minimo que exige la prioridad MoSCoW
#
# El criterio D0R de la guia de la Entrega Final exige:
#   - todo requisito Must   -> estado 'verificado'
#   - todo requisito Should -> estado 'implementado' o 'verificado'
#   - los Could pueden quedar 'pendiente' si se justifican en trabajo futuro
#
# `scripts/validate-traceability.sh` lo comprueba de forma automatica y falla si
# encuentra un requisito por debajo de su minimo que no este listado aqui.
#
# El proposito de este fichero es que la deuda quede EXPLICITA y auditable en
# vez de invisible. Listar un requisito aqui no lo da por verificado: lo declara
# como no verificado, con una razon concreta y una condicion de cierre.
#
# Formato: REQ-ID  # motivo por el que no alcanza su estado minimo
#
# Regla de uso: se admite una excepcion cuando la verificacion depende de una
# condicion externa al codigo (un despliegue, un tercero, un instrumento de
# medicion), o cuando el equipo decide conscientemente dejar fuera de v1.0.0 un
# requisito no critico. Si lo unico que falta es escribir la prueba, no es una
# excepcion: es trabajo pendiente.

# REQ-NF-001a (Must) ya no esta aqui: scripts/verificar-tls-ssllabs.sh fue registrado como
# prueba_automatizada en matriz.csv (el validador solo exige cadena no vacia, no una clase JUnit).
# Analisis SSL Labs ejecutado y archivado el 2026-09-10 contra artisync-frontend.onrender.com:
# grade A+; curl confirma 301 http-a-https. Sube a 'verificado'.
#
# REQ-NF-001b (Must) ya no esta aqui: mismo cierre que REQ-NF-001a. SSL Labs solo reporta
# TLS 1.2/1.3 aceptados (ver ssllabs-api-response-20260910.json). Sube a 'verificado'.
#
# REQ-NF-001c (Must) ya no esta aqui: mismo cierre que REQ-NF-001a. El servidor negocia
# TLS 1.3 por defecto (forwardSecrecy=4 en SSL Labs). Sube a 'verificado'.

REQ-NF-009  # (Must) Disponibilidad y reinicio automatico ante fallos. La politica `restart: unless-stopped` y los healthcheck estan declarados en los cinco servicios de artisync/docker-compose.yml. Demostracion real ejecutada el 2026-09-10 contra el entorno local (docker kill sobre pfc_backend y pfc_postgres, Docker Desktop/WSL2): el reinicio automatico NO se disparo en ninguno de los dos casos en ~70-150s de espera - ver docs/mediciones/resiliencia/REPORTE-RECUPERACION.md. Esto es un hallazgo activo confirmado, no evidencia pendiente de algo que funciona; no se puede extrapolar al despliegue real en Render, que no usa docker-compose para gestionar sus propios servicios. Cierre: investigar la causa raiz del fallo de reinicio en Docker Desktop/WSL2 (o migrar la demostracion a un entorno Linux nativo), y/o demostrar la resiliencia real de Render por otro medio (reinicio forzado desde su dashboard + healthcheck). Script reejecutable: scripts/demo-recuperacion-docker.sh.

REQ-F-022b  # (Should) Un ticket de revision que supera el limite configurado genera automaticamente un enlace de pago (PagoTicketRevisionServicioImpl, entidad propia pagos_ticket_revision para no competir con el escrow principal), probado con PagoTicketRevisionServicioImplTest. Un Should en estado 'implementado' ya cumple su minimo sin excepcion obligatoria; se declara aqui por honestidad. Cierre: ejercitar el flujo completo contra el sandbox real de PayPal (hoy solo probado con respuestas mockeadas) para subir a 'verificado'.

REQ-F-022c  # (Should) Un ticket de revision sin pago tras 48h se rechaza automaticamente vía un 5º scheduler nuevo (TicketRevisionExpiracionScheduler/Servicio), probado con TicketRevisionExpiracionSchedulerTest, TicketRevisionExpiracionServicioTest y TicketRevisionRepositoryIT (la query JPQL con LEFT JOIN ON explicito se valida contra un motor JPA real, no solo con mocks). Un Should en estado 'implementado' ya cumple su minimo sin excepcion obligatoria; se declara aqui por honestidad, mismo motivo que REQ-F-022b. Cierre: mismo que REQ-F-022b -- verificacion contra el sandbox real de PayPal.

# REQ-NF-005 (Should) ya no esta aqui: ChatWebSocketLoadIT (nueva, @SpringBootTest
# con 10 conexiones STOMP reales concurrentes, 5 rondas) mide la latencia extremo-a-
# extremo real: p95=346ms, max=346ms, bajo el umbral de 500ms. La prueba tambien
# expuso y forzo a corregir un defecto real (REQ-F-014/015: @AuthenticationPrincipal
# no se resolvia en @MessageMapping sin registrar AuthenticationPrincipalArgumentResolver
# + SecurityContextChannelInterceptor en WebSocketConfig -- el envio STOMP fallaba
# en produccion para cualquier cliente real, invisible a las pruebas unitarias
# existentes porque invocan el controlador directamente en Java). Sube a 'verificado'.
#
# REQ-NF-006 (Should) ya no esta aqui: ContratoPdfTimingIT (nueva, @DataJpaTest contra
# Postgres real) cronometra 5 generaciones reales de PDF: 1218/29/30/20/20 ms, muy bajo
# el umbral de 5000ms. Sube a 'verificado'.

REQ-NF-017  # (Should) Usabilidad SUS medida en 61.25/100 (docs/mediciones/sus/REPORTE-SUS.md, sesiones del 2026-08-16), por debajo del umbral propio del proyecto (68). Un Should en estado `implementado` cumple su minimo formal sin excepcion obligatoria, pero se declara aqui por honestidad: el resultado medido no alcanza el objetivo, y eso es una brecha real, no solo una brecha de documentacion. El dato 61.25 es el real y verificado (ver docs/mediciones/sus/PLAN-MEJORA-SUS.md: se detecto y corrigio un problema de integridad en sus-raw.csv que antes daba un 76.88 falso) y se publica tal cual, sin sustituirlo. Los items peor puntuados son Q10 (2.06/4, "necesite aprender muchas cosas antes de poder empezar a usar el sistema"), seguido de Q6 y Q8 - patron de carga de aprendizaje inicial, no una funcion puntual rota. Cierre (ver docs/etica/INFORME-SITUACION-ESTUDIO-USABILIDAD.md §3): el equipo aplicara cambios concretos de UX (onboarding, ayuda contextual, menos pasos en tareas guiadas) y medira una ronda adicional DESPUES de ese cambio real y documentado, con aprobacion previa esta vez fechada antes de la recoleccion, consentimiento individual y n>=15; se reportara el resultado exista lo que exista, sin sobrescribir la referencia del 2026-08-16.

REQ-NF-019  # (Must) Idempotencia por estado implementada y probada (PagoServicioImplWebhookTest.reintentoNoDuplica); reconciliacion activa (ReconciliacionPayPalScheduler/EjecutorServicio) y flujo de reembolso/liberacion ante cancelacion con fondos en escrow (PagoServicioImpl.cancelarPedidoConFondosRetenidos) ya existen y estan probados unitariamente (PagoServicioImplCancelacionTest, ReconciliacionPayPalSchedulerTest, ReconciliacionPayPalEjecutorServicioTest), pero contra respuestas de PayPal mockeadas, no contra el sandbox real. Es el requisito de mayor riesgo financiero del corpus (patron escrow), asi que no se sube a 'verificado' solo con prueba unitaria. Cierre: ejercitar el flujo completo (reconciliacion de un pago con webhook retrasado, reembolso de una captura real) contra api-m.sandbox.paypal.com y archivar la evidencia.

# REQ-NF-020 (Should) ya no esta aqui: se introdujo un hash de CONTENIDO nuevo y separado
# del hash de firma existente (este ultimo solo es una huella evento/quien/cuando con
# Instant.now(), nunca reproducible, y se deja intacto con ese proposito); el nuevo hash
# se calcula una sola vez, sobre el HTML ya renderizado y congelado, al completarse la
# segunda firma. ContratoIntegridadScheduler reverifica todos los contratos firmados cada
# noche y POST /api/v1/admin/contratos/{id}/verificar-integridad permite una verificacion
# puntual. Probado con ContratoServicioImplTest (congelar al firmar, integro, discrepancia),
# ContratoIntegridadSchedulerTest y ContratoIntegridadControladorTest. Sube a 'verificado'.

# REQ-NF-024 (Should) ya no esta aqui: restauracion de prueba ejecutada el 2026-09-10 sobre el
# ambiente local de la Entrega Final. Respaldo FULL id=1 (respaldo_full_20260910_1230.dump, 349 KB,
# 895 ms) generado por el panel admin /admin/respaldos; pg_restore exit 0 sin errores; validacion
# post-restauracion: pedidos=4, usuarios=10, contratos=3. Evidencia: docs/despliegue/BACKUP.md
# §Registro de restauraciones de prueba. Sube a 'verificado'.

# REQ-F-010 (Should) ya no esta aqui: AdminComentarioControladorTest (nuevo) cierra el
# unico hueco real que quedaba (ComentarioPortafolioServiceImpl/Controlador ya estaban
# bien probados desde antes). Sube a 'verificado'.
#
# REQ-F-033 (Should) ya no esta aqui: InfraccionServiceImplTest se amplio con
# listarInfracciones y revertirSuspension (los dos metodos que faltaban). Sube a 'verificado'.
#
# REQ-NF-018 (Must) ya no esta aqui: PrivacidadServiceImplIT (nueva, @DataJpaTest contra
# Postgres real via docker compose -f artisync/docker-compose.yml up -d postgres) ejercita
# el bloqueo pesimista de findByIdParaAnonimizar, la deteccion de pedido en curso y la
# excepcion legal por fondos retenidos contra el esquema real. Sube a 'verificado'.
#
# REQ-NF-022 (Must) ya no esta aqui: AuthRateLimitFilterTest se amplio para cubrir los
# 5 limites reales (login, 2fa, forgot-password, reset-password, registro) en su valor
# exacto y en limite+1, no solo login. Sube a 'verificado'.
